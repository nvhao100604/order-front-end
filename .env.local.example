# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
# Đường dẫn cơ sở đến FastAPI Backend. 
# Mặc định trong môi trường dev thường là http://localhost:8000/api/v1/
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1/

# -----------------------------------------------------------------------------
# PERFORMANCE & TIMING SETTINGS (Units in milliseconds)
# -----------------------------------------------------------------------------
# Thời gian chờ tối đa cho một yêu cầu API trước khi ngắt kết nối
NEXT_PUBLIC_API_TIMEOUT=8000

# Thời gian trễ giả lập để kiểm tra trạng thái Loading của UI (tùy chọn)
NEXT_PUBLIC_RESPONSE_DELAY=2000

# Khoảng thời gian tự động làm mới dữ liệu (SWR/Polling)
NEXT_PUBLIC_REFRESH_INTERVAL=60000

# -----------------------------------------------------------------------------
# OTHERS (Add more variables as your system grows)
# -----------------------------------------------------------------------------
# Ví dụ: NEXT_PUBLIC_SUPABASE_URL=your_supabase_url